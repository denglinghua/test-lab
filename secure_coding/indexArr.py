def get_value_from_array0(array, index):
    if index < 0 or index >= len(array):
        return None
    else:
        return array[index]

def get_value_from_array1(array, index):
    # Return the element from the array at the specified index
    return array[index]